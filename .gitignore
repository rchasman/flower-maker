node_modules/
target/
dist/
.logs/

.env
.env.local
.env*.local

# WASM pkg is committed for Vercel builds (no Rust toolchain in CI)
# Rebuild locally with: cargo build + wasm-bindgen
!client/src/wasm/pkg/
.vercel

# Rust build artifacts (not WASM output)
*.rlib
