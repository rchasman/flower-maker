[package]
name = "server"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
flower-core = { path = "../../crates/flower-core", features = ["spacetimedb"] }
spacetimedb = { version = "2.0.5" }
log = "0.4"
serde_json = "1"
