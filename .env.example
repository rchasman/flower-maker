# API — required for AI flower generation
ANTHROPIC_API_KEY=
# API — required when the jev model is selected in the designer
TYPESAFE_API_KEY=

# SpacetimeDB — override static defaults if needed
# SPACETIMEDB_URI=ws://localhost:9300
# SPACETIMEDB_MODULE=flower-picker

# Client (prefix with VITE_ for browser access)
# VITE_SPACETIMEDB_URI=ws://localhost:9300
# VITE_SPACETIMEDB_MODULE=flower-picker

# Agent
# AGENT_NAME=flora-bot
